# Everyday-arrangement Mission

## Mission Brief

**Mission identity:** `Nect/everyday-arrangement`; same sole primary executor, same Task and branch.

**Final Goal:** Make ordinary overlapping/imported artwork easy to arrange and reorganize through semantic Session commands, preserving surviving object identity, references and reversible native state.

**Completion conditions:** Front/back and one-step stacking changes work for selected sibling objects and Groups with predictable multi-selection order, context menu/keyboard access, one-step Undo and native persistence. Actual Windows overlapping-artwork evidence. Add safe neutral-Group ungrouping for imported nesting with atomic refusals for appearance/dependency loss.

**Constraints / Authority:** Explicit continuous-development user instruction and accepted expert direct-edit direction. Working branch commit/push allowed; no main merge/release, no new dependency/native format change. GUI/API/MCP share core authority. No successor Task. Completed vector-intake evidence is in `docs/first-usable.md` and Git.

**Coarse checkpoint map:** (1) Expose existing ReorderObjects semantic authority as practical selection-relative stacking actions. (2) Neutral static Group ungrouping with transform/order/reference preservation. (3) Reassess actual direct-edit workflow.

## ACTIVE CHECKPOINT

**Goal:** Ungroup ordinary neutral static Groups while preserving child geometry, stacking, IDs and references; explicitly reject unsafe compositing/dynamic cases.

**Current phase:** Stacking vertical checkpoint accepted; saving/synchronizing before Ungroup implementation.

**Proven:** Stacking focused3/3 and actual Windows shortcuts/context/Undo/native reopen accepted; see `docs/first-usable.md`. Live core has GroupContiguous/PutInside but no Ungroup. Existing affine/value validation and atomic Session batches can preserve imported static SVG nesting.

**Next task:** Define bounded ungroup contract and add core/API/MCP/GUI route. Preserve static group affine in inheriting child matrices, retain explicitly external child parents. Refuse Group compositing/visibility/dynamic transform loss and references to the removed Group. Validate surviving evaluated values/world transforms.

**Approach:** No native format or dependency change. Reuse existing candidate transaction, matrix compose and scalar mutation checks. Keep IDs/contours, replace the group in sibling order with its children, prune removed-group Collection membership. One Undo restores the complete container.

**Done for next:** Imported nested artwork can be ungrouped from GUI and semantic API with exact geometry/native/Undo checks; unsafe cases fail atomically; actual Windows evidence; coherent commit/push and remote equality.

**State:** `D:\Documents\Nect`, `codex/practical-alpha`; base `d34dd8c41b147be08bdbd80888b0689f83508579`, accepted stacking changes awaiting commit. Owned GUI closed; Full access/never approvals.

**Authority:** Explicit continuous-development user instruction; working-branch commit/push authorized, no main merge/release. Continue across Mission/checkpoint boundaries in this Task.

**Handoff:** CONTINUE_CURRENT_TASK.
