# Practical-alpha Mission

## Mission Brief

**Mission identity:** `Nect/practical-alpha` — durable owner: this file on `codex/practical-alpha`. Every successor Task belongs to this same Mission.

**Final Goal:** Make Nect usable for everyday 2D graphics production as a practical alpha.

**Completion conditions:** A representative editable composition can be created and revised through the Windows GUI, protected/saved/reopened, and exported without loss of native intent. Geometry, Text, images, color and compositing work together; GUI/API/MCP share Session semantics and unsupported requests fail explicitly. Current contracts pass and representative warm interaction meets the recorded 30 fps floor. Close concrete daily-workflow blockers, not the whole backlog.

**Constraints / authority:** The 2026-09-22 user instruction resumes the Mission and supersedes the image-slice stop. Astra is primary executor. Preserve existing work; reversible local UI decisions and checkpoint commits/pushes to `codex/practical-alpha` are authorized. No main push/merge, release, new dependencies, proprietary assets or account-policy changes. Notion owns product intent; `docs/product-direction.md` is the accepted scoped extract. Native authored state remains authoritative.

**Coarse checkpoint map:**
1. Live capability/gap reassessment and image-workflow baseline — completed; evidence in `docs/first-usable.md#practical-alpha-live-reassessment--2026-09-22`.
2. Gentle Snap for everyday object placement — active.
3. Close the next demonstrated revision/output handoff gap. SVG exists; object duplication, PNG output and vector import do not. Choose one bounded slice from use, with explicit reference/unsupported semantics.
4. Integrated authoring, recovery, output and performance acceptance; reconcile remaining limits against practical-alpha completion.

Far checkpoints remain provisional. Inherited Artboard content, full AI/PSD, full node graphs, all blends, pixel painting and all Candidates are not automatic gates. The current capability/gap table lives in `docs/first-usable.md`, not a copied backlog here.

## ACTIVE CHECKPOINT

**Goal:** Make ordinary object placement easier with a visible Snap ON/OFF control and gentle screen-space attraction, preserving exact numeric/API edits and one-gesture Undo.

**Current phase:** Complete; saving and synchronizing this checkpoint before successor selection/dispatch. Snap implementation is complete in this Task; do not implement the next checkpoint here.

**Proven:** `docs/first-usable.md#gentle-object-placement-snap--2026-09-22` owns the compact acceptance and limitations. Current Release build, 112 Canvas checks, batch/transform UI contracts and 70 isolated Image checks pass. Real Windows Canvas checks (113 with screenshot), production Window ON/OFF/Undo and API/native coordinate readbacks verify the actual interaction. Mixed-image Snap ON benchmark meets 30 fps interval and release budgets. 80-curve intervals meet 30 fps, with a recorded 33.85 ms object-move release. The prior intermittent image-chooser timeout recurred in the initial suite; isolated rerun passed, root cause remains unknown.

**Next task:** After checkpoint commit/push equality, select and save the dedicated object-duplication checkpoint from the live core/Window capability gap, then create its fresh Task. Verify Full-access startup as required by the latest user instruction.

**Approach:** Preserve native state and existing shared Session semantics. Next Task receives only Mission Brief, active checkpoint, repo/branch/HEAD and relevant owners; no old conversation or raw logs. Transfer the single writer once.

**Done for next:** Snap is implemented, verified and documented. Synchronize a coherent commit, replace this completed checkpoint with the next selected active checkpoint, synchronize the handoff edit and dispatch a new Task. No next-feature implementation in this Task.

**State:** `D:\Documents\Nect`, branch `codex/practical-alpha`, upstream `origin/codex/practical-alpha`. Starting clean HEAD `c723a678349769a86145aa9029ea5c9ea56d97e2`; completed checkpoint SHA is the commit containing this file. Native 0.13 unchanged. Only owned Snap implementation/tests/docs changed. Owned visible Window and benchmarks are closed, with no pending Session mutation; examples unchanged. Remote pointer: https://github.com/46slv/Nect/tree/codex/practical-alpha. Verify local/remote equality after push.

**Authority:** Current `Nect/practical-alpha` Mission, this Task's Snap delegation, current AGENTS.md and accepted Snap direction. One semantic checkpoint per Task is mandatory. Commit/push of the working branch and fresh successor creation are authorized; main/release/dependency/policy changes are not. User further requires Full access with no approvals for new Tasks/chats. User config already declares `approval_policy="never"` and `sandbox_mode="danger-full-access"`, but this Task received a restricted runtime; the create_task capability has no explicit permissions field. Successor must first report its actual runtime permissions and stop before implementation if they remain restricted. Do not treat prompt text or defaults as proof of runtime Full access.
