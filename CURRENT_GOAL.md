# Stroke-authoring Mission

## Mission Brief

**Mission identity:** `Nect/stroke-authoring`; same primary executor and Task.

**Final Goal:** Make common rounded, square-ended and beveled line artwork editable and consistent across Canvas/PNG/SVG, without changing source geometry.

**Completion conditions:** Explicit retained cap/join/miter contract with compatible versioning; shared commands/API/MCP, Inspector, real Windows rendering and SVG intake/output. Undo/native/recovery preserve style; unsupported cases remain explicit.

**Constraints / Authority:** Continuous-development instruction, accepted ordered Fill/Stroke product direction and interoperable2D workflow. Existing Qt/core stack; no dependencies, release/main merge or successor Task. Preserve old documents' exact default behavior; avoid broad paint framework/native redesign.

**Coarse checkpoint map:** (1) Establish bounded versioned stroke-style contract, core/native/renderer/export and focused evidence. (2) Inspector and SVG import workflow, actual Windows/persistence acceptance. Distant dash/pressure/variable-width/brush work is outside this Mission.

## ACTIVE CHECKPOINT

**Goal:** Define and implement retained butt/round/square caps, miter/round/bevel joins and miter limit consistently in existing stroke owners.

**Current phase:** Selection-transform accepted; saving final GUI checkpoint. Live code hardcodes butt/miter/4 in QPen, path stroker and SVG output; SVG import rejects other styles. Bounded official Qt/W3C research delegated read-only to Luna; primary owns implementation.

**Proven:** `docs/first-usable.md` records selection-transform core full39/39, GUI tests and actual Windows exact matrix/Undo/Redo/native/export acceptance. Prior responsive-editing visible30fps/release floor pass retained;60fps false.

**Next task:** Inspect ShapeOperation codec/versioning and paint projection contracts; combine official cap/join edge-case findings with live code before deciding minimal compatible representation.

**Approach:** Extend existing versioned stroke operation and shared paint evaluation only. Keep old fixed-style operations unchanged; explicit unsupported version/refusal beats silent style loss. Verify geometric paint probes and SVG attributes before UI claims.

**Done for next:** Native/version/semantic contracts and renderer/export consistency verified; checkpoint synchronized, then finish UI/import production route in this Task.

**State:** `D:\Documents\Nect`, `codex/practical-alpha`; base `56a951fc5b455971b3a9605c1a43b9dd6fac5bae`, selection-transform GUI accepted and ready to commit. Owned GUI closed, no test/build running. Full access/never approvals; native0.13 currently.

**Authority:** Explicit continuous accepted-direction development and bounded lightweight research; working-branch commit/push allowed. No Task rotation.

**Handoff:** CONTINUE_CURRENT_TASK.
