# P02-B implementation packet — layout overlays and setup

**ID / revision / kind:** `P02-B-ENTRY-01` / 1 / approved bounded implementation packet. **State:** `CLOSED / REMOTE_SYNC_VERIFIED` at `codex/practical-alpha@68a700f2be1f9550ead9c0f911cc0d04350bd66d` (local/remote match; clean tree). **Mission:** `Nect/practical-alpha`. **Exact P02-A source baseline:** `D:\Documents\Nect`, `codex/practical-alpha@d21c968e309abdbccec061721e6906ecaee1a020`; dispatch HEAD was `d29d87931efe4b74923c88d22b4e8ffc27c1bec6` after the packet commit. The fresh Worker implementation and Sol repair are complete.

**Authority:** [DEC-70](https://app.notion.com/p/3e6fd279a6f3811c9448f8bfb8543bde), [DEC-71](https://app.notion.com/p/3e6fd279a6f381219079f6b9a024fece), current user Mission ownership, [`P02-ENTRY-01` revision 5](p02-implementation-entry.md), and repo `AGENTS.md`. P02-A native 0.14 is implemented and Sol-reviewed: full Release build and serial CTest 41/41 PASS; semantic external JSON Schema validation was NOT_RUN because `jsonschema` is absent. P02-B contributes REQ-163's on-demand Guide/Margin/Grid creation, preview and revision plus visible layout references needed by REQ-32. It does not close REQ-32 object/point snapping, REQ-33, REQ-95, or the P02-D Utility Strip.

## Ownership and bounded scope

- Sol owns this packet, integration, exact candidate review, GUI/runtime evidence, repair decision, Git and `CURRENT_GOAL.md`. One fresh disposable Luna-Max Worker owns only `src/desktop/canvas.{hpp,cpp}`, `src/desktop/window.{hpp,cpp}`, focused `tests/canvas_tests.cpp` and `tests/window_tests.cpp`; `src/desktop/host.cpp` and its test only if a demonstrated recovery/readback gap needs repair. The Worker does not change core/native schema, unrelated UI, docs, Git refs, or user files. It is not alone in the repo and must preserve other edits.
- Canvas projects committed or validated preview Guide/Grid/Margin state from the shared Session. It owns per-window overlay visibility and transient hit/hover state, never a second authored layout model. Window offers the editing controls in the existing active-frame/Properties surface. Build reusable controls or helpers so P02-D can place them in the accepted Canvas-adjacent Utility Strip; do not implement the Strip here.
- Protected surfaces: existing 0.1–0.14 schema and fixture bytes, user projects/settings, output artwork, existing object Snap behavior, stable IDs, recovery authority, export semantics, and all unrelated keyboard shortcuts. No workspace persistence, property pin/search extension, Node drawer, dependency/API addition, or P02-C snap/align/distribute implementation.

## Interaction and state contract

1. Draw x Guides as constant world X and y Guides as constant world Y across the active Composition viewport. Draw active Artboard Margin as its evaluated inset rectangle. Draw each visible Artboard Grid at evaluated Artboard origin plus its local bounds; show rectangle, row/column cell edges and gutter boundaries. Clip Grid to its own bounds. Draw on the Canvas after artwork without entering native, SVG or PNG export; selection and authored object geometry remain unchanged. Use legible low-noise strokes at 100% and 200% DPI. An empty document has no spurious overlay.
2. `show_guides`, `show_grid` and `show_margin` are independent per-window view booleans, default ON when their authored definitions exist. Toggling them changes no Document revision, Undo, recovery bytes or export. Existing Snap enable state remains independent; hiding an overlay must not disable any existing snap, and P02-C will add Guide/Grid candidate toggles and feedback. Do not display a Guide/Grid snap control as functional before P02-C supplies its behavior.
3. Existing active-frame settings expose Margin inset fields; Grid bounds/count/gutter fields; an explicit one-time **Set Grid to margin box** action; and a Composition Guide list with add/edit/delete, axis and position. Labels include du units and stable human-readable names. Numeric fields commit on Enter or explicit Apply; focus alone or a visible draft is not a commit. Clearing Margin preserves Grid; clearing Grid preserves Margin; clearing the last component sends null layout. Guide add uses `new_id()` and successful edit/delete retains or removes only its stable ID. Existing Grid edits retain its ID; first creation allocates one.
4. The copy action requires an authored Margin. It calculates `{left,top,width-left-right,height-top-bottom}` from the evaluated active Artboard and writes those four Artboard-local Grid bounds in one `SetArtboardLayout` command, preserving existing Grid counts/gutters/ID or creating an ID with valid first-slice counts 1×1 and zero gutters. It does not create a persistent link. A later Margin edit leaves Grid numbers unchanged. Without Margin, reject `INVALID_LAYOUT` visibly and leave revision/history/state unchanged.
5. A Guide position drag occurs only in explicit Guide edit mode so object/point drags keep their existing routing. Hit test is 6 logical px at any DPR; an exact-distance tie chooses lexicographic Guide ID. Start freezes Session/document identity, revision, Guide ID, axis, zoom and authored snapshot. Preview is transient through the same Session gesture; Escape cancels without revision/history. Release commits one Undo. If API/Undo/document switch advances identity or revision, reject stale commit, discard preview, refresh and require a new gesture. Invalid numeric/layout previews leave the last valid preview visible and do not commit.
6. Window editing uses `AddGuide`, `UpdateGuide`, `DeleteGuide` and a complete `SetArtboardLayout` payload through the existing Session/Host notification path. Validation errors are visible near the control or status, and failed edits restore the committed field display. Undo/Redo, save/reopen, recovery and API inspect read the same accepted definitions. Artboard resize that violates Margin/Grid is rejected atomically with no scale/clamp. Do not silently coerce a fractional count, negative gutter, nonfinite value, wrong plane, duplicate ID or stale revision.

## Positive and negative fixtures

| Fixture | Expected result |
| --- | --- |
| `B-GRID-01` | On Artboard `(0,0,960,640)`, enter Margin `(40,20,40,20)`, create 2-column Grid, gutter 20, and copy margin box. Local bounds become `(40,20,880,600)` and X cells `[40,470]`, `[490,920]`. Move Artboard x to 200: visible world cells become `[240,670]`, `[690,1120]`; object coordinates do not change. |
| `B-INDEPENDENT-02` | Change Margin left 40→50 after copy. Grid local x stays 40. Hide/show each overlay independently: Document revision/history, native/recovery bytes and SVG/PNG artwork do not change. Save/reopen retains authored IDs/numbers; per-window visibility remains view state. |
| `B-GUIDE-03` | Add x Guide at 100 and y Guide at 250, preview x Guide drag to 120, release: only x Guide moves and one Undo restores 100. Escape on a second drag adds no history. Renaming/reordering Artboards does not retarget either Guide. |
| `B-NEG-04` | Copy with no Margin, resize width 960→900 while Grid right is 920, enter left+right=width, zero-width cell, fractional count, negative gutter, duplicate ID and stale revision: explicit error; document, revision, history cursor/redo, recovery and object geometry unchanged. |
| `B-STALE-05` | Begin a Guide drag at revision r, perform a separate accepted API edit at r, then release: `REVISION_CONFLICT` or session conflict, no blind rebase, Guide remains at its last committed coordinate. Hide overlay during drag or switch document: preview cancels safely. |

## Acceptance and next gate

Worker supplies focused Qt Canvas/Window positive and negative tests and exact commands/results. Sol reviews identity, coordinate transforms, draft/revision guards, one-Undo gesture, no export ink, no second state, and source diff. Run Release build and focused tests after repair. Then launch the actual Windows Desktop on an owned disposable native document; record executable path/hash, document/session/revision, screenshot or frame evidence at 100% and 200% DPI where supported, creation/copy/drag/cancel/visibility/save/reopen/Undo/recovery readback, and real p50/p95 frame intervals against `p95 <= 33.3 ms` on the bounded scene. Mark unavailable host routes `BLOCKED_ENV`, not PASS. Preserve user projects and settings.

**Explicit next gate:** Sol accepts only an exact P02-B candidate with passing focused tests and real applicable GUI/runtime evidence, then commits, non-force pushes, reads back remote SHA and updates the single active checkpoint to P02-C. Normally continue in this same Sol Task. P02-C adds actual object/point Smart Snap and explicit align/distribute; P02-D adds the Utility Strip, Repeater angle controls and G alias/help. Neither is claimed by P02-B packet approval.

## Sol acceptance record — 2026-09-25

- Exact candidate: `src/desktop/canvas.{hpp,cpp}`, `src/desktop/window.{hpp,cpp}`, `tests/canvas_tests.cpp`, `tests/window_tests.cpp`, and Sol's visible-layout scenario in `tests/canvas_benchmark.cpp`. Sol inspected identity, world/local coordinates, gesture/Undo, export isolation and draft guards. Sol repaired exact Guide hit ties and fail-closed Inspector behavior when a draft or untouched editor was created at an older Session revision.
- Release build PASS. Serial `ctest --test-dir build -C Release --output-on-failure` PASS 41/41 after production repairs. The final recovery assertion changed only `tests/window_tests.cpp`; focused `window_interaction` PASS after that assertion. Core P02-A tests cover invalid bounds, count, gutter, duplicate ID, wrong plane and atomic Artboard resize. B-focused Qt tests cover overlay/export isolation, transient preview, one Undo, Escape, hidden overlay, stale session, numeric invalidity, stale draft/editor and exact recovery readback.
- Actual Windows GUI, disposable document `build/p02b-gui-disposable.nect`: executable `build/Release/nect_desktop.exe` SHA-256 `CF8ADE54DFBD38E61C0770E41294B391B5E209DAC5EAE08D740C166BEED1171B` at the start of GUI validation; the later stale-guard rebuild was verified by Qt tests, not rerun manually. At 96 logical DPI, GUI added Margin `(40,20,40,20)`, copied Grid bounds `(40,20,880,600)`, set columns 2/gutter 20, previewed and committed an x Guide at 100, dragged it to `140.796812749004`, hid/showed Guide without revision, changed Margin left to 50 while Grid x remained 40, saved native v0.14 and reopened it. Reopened Inspector and native bytes agreed on Margin left 50, Grid x 40 and stable Guide ID/position. Session status showed r6 and Recovery r6. The owned Qt recovery test loaded the exact protected native bytes and reopened the recovery, preserving Guide/Grid/Margin IDs and values.
- A process-only `QT_SCALE_FACTOR=2` Nect launch succeeded, but the host placed the large window partly offscreen (`origin 2559,-31`, capture `1281x1801`); the Properties dock and full Canvas were inaccessible for reliable 200% visual acceptance. This row is `BLOCKED_ENV`, with no 200% product PASS claim. The 96-DPI GUI path and scale-independent Qt Canvas checks are PASS.
- Visible production Window benchmark `build/p02b-layout-benchmark.json` PASS: two scenes, 2 and 80 paths, four Guides plus Margin and 2×2 Grid, 10 pan/zoom/point/handle/object operations. All p95 frame intervals were `17.53–21.25 ms`, with zero intervals above 33.333 ms in each 89-interval operation; `all_operations_meet_30fps_p95_interval_budget=true`. Windows 11, Qt 6.5.3, DPR 1, 2560×1440/30 Hz reported. GPU presentation and 200% DPI timing were not measured.
- Residuals: P02-C Guide/Grid/object/point Snap and align/distribute, P02-D Utility Strip/Repeater/G alias, and REQ-33 baseline residual remain open. Subjective hands-on visual feel is reserved for its Human Gate. P01 R1/R5/R8 `BLOCKED_ENV` remain a separate qualification route and do not block this P02 candidate.
