# R03-BASELINE-01 — subsequent-line Text baseline Snap

**Packet:** revision 1, Sol-approved bounded implementation. **Source:** `D:\Documents\Nect`, clean `codex/practical-alpha@6e0ebc18bda37890cbdd1785c106ebedf9f2bdd3`, local/tracking/fresh remote matched before freeze. **Authority:** DEC-71 Mission, R03 Route, Confirmed Must REQ-33, P02-C1 closure, repository `AGENTS.md`. The single-writer takeover ACK is `build/r03-baseline-takeover-ack.json`.

## Bounded result

For a visible, stationary, same-scope, horizontal axis-aligned Text target, offer each measured DirectWrite line baseline as a Y Smart Snap target. This packet adds the **second line** acceptance. A single moving horizontal axis-aligned Text still uses only its measured first-line baseline as the source. Its first line may snap to a target's second line; feedback names the target and `line 2 baseline`. Keep the existing first-line target label, object bounds candidates, distance-first 6 logical-pixel inclusive threshold at frozen zoom, DPR independence, exact priority and stable tie order. Preview stays transient and release remains one `TranslateObjects` Session transaction and one Undo. No authored text/native 0.14, core alignment, API numeric placement, controls or settings change.

Rotated/vertical target and source Text offer no horizontal baseline candidate. No glyph-box edge or estimated line-height baseline substitutes for a measured line. The packet does not claim arbitrary rotated/vertical baseline, full R03 or full REQ-33 closure.

## Fixed positive and negative oracles

- `R03-BASELINE-01`: two same-font Text objects use content `H\nH` for stationary target and `H` for moving source, both with authored `line_spacing=80`, target origin `(320,96)`, moving origin `(40,80)`, and identity transforms. Measured target line-2 baseline minus measured source line-1 baseline is **96 du**. A Y-only raw body drag of **97 du** (1 du away) snaps to committed `transform.ty=96`; feedback contains `Text baseline → target-text` and `line 2 baseline`. One Undo returns `ty=0`; native save and independent reopen retain the committed `ty=96` on the final placement.
- `R03-BASELINE-NEG-02`: a raw 103-du drag (7 du away) does not use the second-line candidate; a one-line target offers no line-2 label. Vertical or rotated target offers no baseline. Hidden, moving/follower, cross-scope targets do not offer it. Escape or stale release leaves authored bytes/revision/history unchanged. Exact numeric/API translation remains exact without Snap.
- The fixture isolates the second-line candidate from Guide, Grid, Artboard, ordinary edge/center and target first-line corrections within the 6-pixel threshold. Assert the candidate feedback as well as coordinates. Run focused text/Canvas tests and proportionate serial regression, then an owned GUI/API/native save and independent reopen at 96 DPI. Measure the existing visible layout gate if available.

## Ownership

Sol owns `include/nect/core.hpp`, `src/text.cpp`, `src/desktop/canvas.{hpp,cpp}`, focused `tests/{text,canvas}_tests.cpp`, verification, Git and Notion reconciliation. This is a derived layout metric only; the authored model and native format remain intact. Other agents may read the repository; do not revert their edits.

## Candidate evidence and open validation gates (2026-09-26)

Sol reviewed the derived DirectWrite line metrics, axis-aligned world projection, same-scope/visibility exclusions, source-first-line limitation, stable target line order, priority and unchanged Session release path. The first raw 91-du fixture was closer to the ordinary glyph bound edge than the baseline; the fixed 97-du positive oracle respects the required distance-first rule. Release focused `text_contract`, `text_authoring_contract` and `canvas_interaction` passed 3/3. Full serial Release CTest passed **40/41**; `offset_ui_interaction` repeatedly crashed in `Qt6Widgets.dll` (0xc0000005) after Stack Jump's QAction triggered and before the next event drain. The test does not create Text. Temporary diagnostic logging was removed; this crash is not assigned a cause or marked PASS.

Owned 96-DPI GUI fixture `build/r03-baseline-gui.nect` used installed Segoe UI, moving `H` at `(40,80)` and target `H\nH` at `(320,96)` with 80-du authored line spacing. The in-flight GUI status displayed `Y: first-line baseline Text baseline → target-text line 2 baseline`. After Undo of an initial inaccurate manual drag, a fresh vertical-only glyph drag committed `transform.tx=0, transform.ty=96` at API revision 4. Saved native **0.14** SHA-256 `9B42C976BEE84EFF7B828C4C9FE69539B4B6384EF4A399906ED8BA73E7E04BC4` reopened in independent Session `2fc11fe5-5108-4946-8500-6d823742d49d`, retaining Document `2ecc8af7-de9a-42dd-b75a-2567f30c3b06` and `ty=96` at reopened revision 0. Both owned GUI processes were closed. This is actual GUI/API/native evidence for the positive path; feedback's exact line label is also asserted by the focused Qt test.

The initial visible layout benchmark reported representative object-translation interval p95 **36.95 ms**, then **37.45 ms** on a bounded recheck, above its 33.3-ms 30-fps gate; observed host processor load was 82%. To attribute this, Sol extracted unchanged `6e0ebc18…` source via `git archive` into ignored `build/r03-baseline-src`, built it separately with the same Release compiler/Qt/Boost, and ran the same visible scene. The baseline representative object-translation p95 was **20.58 ms** and the immediately following candidate run was **20.16 ms**; both runs met every 30-fps operation gate. This supports a transient host-load cause for the earlier timing RED. The benchmark scene has no Text, and these paired runs do not claim a dense typography stress result.

The unchanged baseline `offset_ui_interaction` also reproduced the SegFault in 4.68 seconds. The candidate had failed in 3.09–4.29 seconds at the same Stack Jump action and Qt6Widgets fault module; the baseline comparison proves the failure predates this slice. The focused baseline acceptance, actual GUI/API/native save/reopen, and paired visible performance gate are PASS. The repository's full serial CTest remains **40/41 RED** due to that independent existing defect, which is carried forward explicitly rather than waived or called PASS. Rotated/vertical baseline behavior, remaining R03 and REQ-33 stay open.
