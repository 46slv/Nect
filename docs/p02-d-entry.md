# P02-D implementation packet — daily UI first slice

**Packet ID / revision / kind:** `P02-D-ENTRY-01` / 1 / Sol-approved bounded IMPLEMENTATION. **Mission:** `Nect/practical-alpha`. **Exact implementation source baseline:** `D:\Documents\Nect`, `codex/practical-alpha@c82bf5606a18027bd6a166fb243d9fb8ed3cde5b`; local HEAD, origin tracking and fresh remote readback matched with clean tree at freeze. The later documentation dispatch commit is a handoff marker, not a replacement source baseline. **State:** IMPLEMENTED / SOL REVIEWED / RUNTIME QUALIFIED; remote closure receipt below.

**Authority and obligations:** current user Mission ownership, accepted [DEC-70](https://app.notion.com/p/3e6fd279a6f3811c9448f8bfb8543bde) and [DEC-71](https://app.notion.com/p/3e6fd279a6f381219079f6b9a024fece), Confirmed [REQ-9](https://app.notion.com/p/3e0fd279a6f38106a3e5cc2d4d8437b2), [REQ-35](https://app.notion.com/p/3e0fd279a6f38139a468f49654968e2f), [REQ-45](https://app.notion.com/p/3e0fd279a6f381f3adf3e90cbd811a31), [REQ-162](https://app.notion.com/p/3e0fd279a6f3810ab72acc0d73a6c2ac), [REQ-164](https://app.notion.com/p/3e0fd279a6f381fcab0cd396763100fc), [REQ-179](https://app.notion.com/p/3e0fd279a6f38190821bf2926a9ee650), [P02-ENTRY-01 revision 6](p02-implementation-entry.md), [P02 design closure](p02-design-gate.md), repo `AGENTS.md`. This slice makes existing P02-A/B/C affordances reachable and adds the accepted Repeater/shortcut adapters. It does not close other Requirement residuals by implication.

## Ownership and surfaces

- **Worker:** one fresh disposable Luna Max code writer. Own `src/desktop/window.{hpp,cpp}`, `src/desktop/canvas.{hpp,cpp}` only if a narrow view/zoom accessor is required, and focused `tests/window_tests.cpp`; state a concrete need before another code/test file. Read `ARCHITECTURE.md`, existing Canvas view/snap methods, `Window::edit_artboard`, `Window::add_property`, Repeater stack branch, Session gesture API and current shortcut actions before editing. Sol owns packet/docs, Git, exact review, GUI/runtime acceptance and checkpoint. Worker must not commit, push, edit Notion or touch user projects/settings. Other agents may read this repo; do not revert their edits.
- **Allowed behavior:** UI-only adapters to existing Session commands/Refs and Canvas view state. Keep same native 0.14 writer/schema/migration, IDs, geometry, Snap arbitration and align/distribute semantics. No dependencies, second Document model, persistent workspace schema, property pin/search extension, Node drawer or blanket shortcut remap.
- **Protected:** C1/C2 code and evidence, current Nect shortcuts, user projects/settings, recovery, exporter/render behavior, credential/font assets and unrelated UI. Build/test outputs stay in ignored `build/`. Sol is the sole Git/worktree integrator; no concurrent code writer.

## Fixed contracts

1. **Canvas-adjacent Utility Strip.** Place a compact, visibly adjacent strip at the Canvas edge, not in a detached Settings dialog. It offers Show Guides, Show Grid and Snap view toggles; Fit/zoom control; active Artboard and output dimensions readback; and an on-demand setup surface for the existing Margin/Grid bounds/counts/gutters/Guides editors. Reuse current `Canvas::set_show_guides`, `set_show_grid`, `set_snap_enabled`, `fit_artboard` and existing layout/Guide Session commands. Show Grid and Snap are independent; hiding an overlay cannot disable its Snap candidate. View toggles/zoom change no Document revision, Undo or recovery. Setup drafts use the existing transient preview/commit path, with stale-session/revision rejection and atomic invalid-layout behavior; a successful authored edit is one Session transaction/Undo and survives native save/reopen. Do not silently reset overlays on setup close. No workspace persistence promise.
2. **Pointer and keyboard states.** Toggle ON/OFF, hover, keyboard focus and disabled states are named and visually distinct; essential state stays legible without hover. Pointer-capable controls use fluid/proximity hover feedback without moving or shrinking their stable hit rectangle. Hover appearance never authors or commits data. At narrow window width, controls remain reachable by wrapping, overflow or scrolling. 100% and 200% DPI are acceptance targets when the host permits actual interaction; an environment geometry block must be recorded separately.
3. **Repeater angle.** A radial knob and precise numeric editor operate on the *same existing* `operation_ref(object.id,operation.id,"rotation")` Scalar Ref (`op.<id>.rotation`). Authored degrees remain signed and unwrapped within the existing finite ±1e9 Scalar bound. Knob indicator displays modulo 360 only; continuous pointer travel accumulates whole turns and adds a signed delta to the gesture-start value. Numeric entry preserves exact 725 rather than normalizing to 5 or 365. One completed drag is one gesture/Undo; Escape cancels without revision. Stale/ref missing/driven/invalid input rejects without partial state; native/API/Inspector readback names the same Ref. Reuse Session gesture/SetValue semantics and update the existing numeric editor after knob changes; avoid a display-only mirror property.
4. **Shortcuts and collision help.** Keep current Nect defaults and add `G` as a second Draw Path shortcut alongside `P`; both invoke the same Draw Path action. Contextual shortcut help explicitly lists at least `P` (Nect Draw Path / AE Position), `G` (Nect Draw Path / AE Pen), and `Ctrl+Shift+T` (Nect rotate/scale / AE Effect Controls). Show Nect's actual binding/action and AE collision plainly. Do not silently remap an existing key; text-field key entry must continue to win while focused.
5. **No core changes.** Guide/Grid/Margin keep P02-A identity, coordinates, bounds validation, native 0.14 migration, command/error/stale/Undo/recovery/persistence rules. Snap keeps minimum-distance, exact tie priority `Guide > Grid > Artboard > Text baseline > object edge > object center > equal-gap`, 6 logical-px threshold, DPR-independent evaluation, and hidden-overlay behavior. C2 Align/Distribute keeps selection fixed outer objects/equal gaps, Artboard/Grid virtual outer edges with `n+1` gaps, explicit Guide-distribution rejection, and fixed key with explicit nonnegative spacing. The strip exposes these semantics; it must not create a second path around them.

## Fixed positive and negative oracles

| ID | Fixture and expected result |
| --- | --- |
| `D-STRIP-01` | With one active Artboard 960×640, a Margin and Grid, strip readback names the active Artboard and 960×640 output. Toggle Guides OFF and Grid ON while Snap stays ON: no revision/history/recovery mutation; hidden Guide snap remains eligible if its Snap category is enabled. Toggle Snap OFF: no Snap candidate, overlay remains visible. Fit/zoom changes view only. Open setup, change Margin left 40→50 and commit: one revision/Undo and native readback 50; Grid copied earlier from margin remains at x=40. Invalid Grid bounds and stale revision reject atomically; Escape/close cancels transient preview without authored change. |
| `D-KNOB-02` | On one Repeater `op.<id>.rotation`, numeric 725° commits exactly 725; indicator reads 5°. Drag +10° commits 735, indicator 15°, exactly one Undo restores 725 and redo 735. A subsequent whole-turn drag adds 360 without wrapping authored value. Escape after a live drag restores its start value without revision/Undo. Negative and near ±1e9 values preserve numeric meaning; NaN, over-limit, stale revision, missing Ref and driven target reject atomically. API/native/Inspector all read the same value. |
| `D-KEYS-03` | Focus Canvas: `P` and `G` each enter the same Draw Path mode and can create a path; Escape exits. Focus a text input: typing `G` edits text instead of entering Draw Path. Help shows the three named Nect/AE collision pairs; existing rotate/scale binding remains `Ctrl+Shift+T`. |
| `D-STATE-04` | Accessible names and visible ON/OFF, hover, focus and disabled treatment exist for strip toggles, knob and setup. Pointer approach and hover never changes the hit rectangle, document revision or numeric value. At 100% and 200% DPI and narrow width, all essential actions are reachable; if the current host cannot support a genuine 200% interactive window, mark that row `BLOCKED_ENV` with geometry evidence, not PASS. |

The Worker should add focused Qt tests for deterministic action/Ref/gesture/state contracts. Sol supplies an actual Windows GUI fixture for interaction, save/reopen, recovery and visible layout. Do not generate expected values from the implementation.

## GUI/runtime acceptance and next gate

Sol reviews the exact diff and tests for one Ref, gesture atomicity, stale/cancel/error paths, existing shortcut preservation, text-input priority, independent overlay/Snap state, accessible control states, no layout clipping and no native/core drift. Run focused Release Desktop/Window tests and proportionate serial regressions. On an owned disposable native document in the actual Windows Desktop, exercise strip view toggles, setup preview/commit, Repeater numeric 725→knob 735→Undo, `P`/`G`, help, focus/hover and native save/reopen/recovery. Record executable hash, Session/Document IDs, revision, DPI and genuine environment blocks. Keep P01 R1/R5/R8 `BLOCKED_ENV` distinct. Reuse the existing visible Canvas performance evidence unless this change reaches its hot path; if it does, capture the 30fps `p95 <= 33.3 ms` target again.

**Explicit next gate:** Worker returns a bounded candidate and evidence without Git mutation; Sol reviews/repairs, verifies actual GUI/runtime, accepts or returns for repair, commits coherently, non-force pushes `codex/practical-alpha`, reads back exact remote SHA and clean tree, and updates `CURRENT_GOAL.md` plus Notion. Then fresh-read the Completion Route and live Requirements, select the highest-value eligible next step under DEC-71. Rollover is conditional on material context cost, phase independence, capability/authority boundary or safety advantage; this packet alone is not a Task boundary.

## Closure receipt — 2026-09-26

**Code candidate:** `eea3ce67edf39ce166c9d084bcaccf3842283c9f` implements the UI-only adapters in the five allowed files. Sol reviewed the exact diff for shared `op.<id>.rotation` Ref, Session gesture/Undo, stale cancellation, Canvas view state, anchored setup and unchanged native/core semantics. The focused negative-fixture follow-up is `8abfbab7eb27aecee4bcc9828ff5b878aa62616c`. No Worker Git/Notion mutation or second code writer occurred.

**Deterministic acceptance:** Release `window_tests` PASS after the follow-up; it checks the 960×640 strip, independent Guide/Grid/Snap view states with no revision, 1000px overflow reachability and stable hover target, setup preview/Escape/close/one commit/Undo with Grid identity intact, P/G text-field priority and named collision help, exact 725→735 knob/one Undo/full turn/Escape/stale, plus signed near-limit, over-limit atomic reject and driven dial disabled. Serial Release CTest was 41/41 PASS on the code candidate; only the focused test fixture changed afterward. The failed first form of the added driven test attempted a length-to-angle link and correctly received `Implicit unit conversion is not supported`; Sol corrected the fixture to a same-unit rotation source and reran `window_tests` PASS. `git diff --check` PASS.

**Owned Windows GUI/runtime:** Release executable SHA-256 `77D141307895D2AB40BCE2CD5222267C0D51F017816BB4B27B0624D3A50173EF`, 96 system DPI. The disposable native file `build/p02d-gui-disposable.nect` retained Document `0d24b4c8-1b55-4ea0-bb70-9db3f5c17f7e`; first acceptance Session `92ffc71d-a80e-4a0f-8c79-8dcc170520c4`. The visible Utility Strip, Setup popup, margin preview/cancel, Guide OFF with Grid/Snap ON and no revision, Keys help, C2-A Repeater numeric 725, actual knob drag to 810.601294645 and one Undo to 725, and Canvas G Draw Path were exercised. Exact Session API readback at revision 4 returned `op.ff91f844-df9b-4e1d-802c-e1eb3e071d08.rotation=725` and unchanged Grid `c2-grid` bounds `(50,50,860,540)`. In a fresh GUI Session, Setup Margin left 24 committed by focused input + Enter at revision 1; Undo at revision 2 removed Margin and kept Grid ID/bounds; Redo at revision 3 restored 24. The saved native v0.14 file SHA-256 is `7c93b2e5395276e08abe6321f03e7ce2afed66d80924150b3bbdde2d78589eaa`. Cold reopen Session `96cced92-b859-4fc9-8255-723aa9a6e443` read Margin 24, Grid `c2-grid` x=50 and Repeater 725 with the same Document ID. Recovery receipt for the first Session at revision 4 and its `.nect` snapshot both match SHA-256 `fd4cfab76ab99d5cdb33bea7ac9cf22abad8b6b24a1418b02962bce744852b5b`. GUI screenshots are kept under ignored `build/p02d-evidence/`; all owned Nect processes were closed.

**Scope/result:** `D-STRIP-01`, `D-KNOB-02`, `D-KEYS-03` and 96-DPI `D-STATE-04` are qualified by focused tests plus actual runtime paths above. A genuine 200% interactive host run remains `BLOCKED_ENV` because this host exposes 96 system DPI; no user display setting was changed. P01 R1/R5/R8 `BLOCKED_ENV` remain separate, and REQ-33 rotated/vertical/subsequent-line baseline and repeated-gap residual stay open. P02 first slice is implemented; this does not declare every linked Requirement or L1/L2/L3 complete.

**Remote closure:** the implementation and evidence commits are to be synchronized with the final checkpoint commit; exact final remote SHA is recorded in the live Mission handoff/Notion readback. The next eligible independent phase is R11-Q early interop/host feasibility qualification, subject to fresh scoped Requirements and packet freeze by the successor Sol Owner.
