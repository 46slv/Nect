# R11-Q feasibility qualification — interop, print and host

**Packet ID / revision / kind:** `R11-Q-ENTRY-01` / 1 / Sol-approved bounded QUALIFICATION + DESIGN_GATE. **Mission:** `Nect/practical-alpha`. **Source baseline:** `codex/practical-alpha@1bb8d6fcb62159b49bd28d99c10d250952fa1c98`; local, tracking and fresh remote matched with a clean tree at takeover. This packet authorizes evidence gathering and disposable probes, not a product codec, print pipeline or host adapter.

## Authority and scope

The [Completion Route](https://app.notion.com/p/3e3fd279a6f381b4ba9dd2d1bf066e0f) opens R11-Q after R01. The [Worker packets](https://app.notion.com/p/3e3fd279a6f381ee8ab0ea5ea614c006) §14 and U09 require a target format/version/direction/feature/reader table, real backend and license/runtime evidence, original/native preservation, and a bounded R11-I contract or explicit design gate. [DEC-71](https://app.notion.com/p/3e6fd279a6f381219079f6b9a024fece) grants Sol Mission ownership for this reversible qualification. The live scoped Requirements include Confirmed Must `REQ-6,68,69,71,72,75,78,80`, Confirmed Should `REQ-11,12,74`, and Candidate interop, print and OpenFX work. Candidate rows remain Candidate.

Sol owns packet, technical judgement, exact source/runtime review, Git/Notion reconciliation and checkpoint. Fresh disposable Workers may gather narrow official-source evidence without Git, Notion or GUI mutation. Sol is the sole writer. Allowed tracked writes for this checkpoint are this packet and `CURRENT_GOAL.md`; add another tracked file only for a concrete reviewed evidence need. Disposable probes and receipts live in ignored `build/r11q/`. No dependency, SDK, codec, native schema, UI, user project, settings, or existing fixture mutation. No external upload, print vendor submission or license/account change.

## Fixed evidence cells

| Cell | Required observation | PASS boundary |
| --- | --- | --- |
| `Q-AI-IN` | AI representation/version sniff, independent source hash, decoder path, scoped Path/Text/Gradient/Image/clip and unsupported behavior | Illustrator-independent decode into editable native structure for a declared subset. PDF rendering or file extension alone is insufficient. |
| `Q-AI-OUT` | Encoder backend/version/license/runtime availability, derivative bytes, source/native hash preservation, external reopen | A distinct AI derivative with target profile/loss report and independent appearance **and** editability readback. An installed app or SVG/PDF output alone is insufficient. |
| `Q-AI-API` | Inspect/decode/plan/encode/validate entrypoints and structured result | Actual headless API/MCP behavior for the AI lane; JSON-lines alone is not MCP. |
| `Q-PDF-PRINT` | PDF target/version, ICC/CMYK/Spot/font/page boxes/overprint or selected job contract, validator and reader capability | Only the explicitly probed cells; no production or PDF/X claim from a generic PDF viewer. |
| `Q-HOST` | Official Resolve/Fusion/OpenFX SDK and host behavior, installed runtime, licensing/packaging, standalone boundary | Owned scratch host registration and actual readback if attempted. Installed binaries or docs alone are feasibility evidence, not host acceptance. |

For each cell record `PASS`, `FAIL`, `BLOCKED_ENV`, `DESIGN_GATE` or `NOT_RUN` separately. Appearance, editable semantics, backend existence, licensing and API exposure never imply each other. Preserve native authored state and immutable source bytes. A safe export bake dependency includes inputs, masks, backdrop, filter support, resolution and color context; it is not assumed to equal one subtree.

## Bounded qualification plan

1. Inventory current Nect native 0.14, SVG import/export, PNG export, API/MCP and installed tool versions; record source pointers and executable hashes where used.
2. Consult current official documentation for AI/PDF representation and available reader/encoder paths, color/print validation, OpenFX and Resolve/Fusion SDK/licensing. Separate documented capability from local availability and tested behavior.
3. Use only owned scratch artifacts in `build/r11q/`. Start with one simple Path/Text/Gradient/Image/clip fixture where Nect can actually express it, and one known AI source produced by an authorized local reader/encoder if available. Hash the original, sniff bytes, reopen with an independent reader, test unsupported/corrupt input rejection, and verify source/native unchanged. Do not infer full feature coverage from this first fixture. Add two Artboards, named objects and multiple paints only after the minimal path is proven.
4. Choose one smallest viable R11-I vertical slice by format/version/direction/features with a positive and negative oracle. If a required reader, encoder, SDK, license or runtime is absent, state the exact DESIGN_GATE/BLOCKED cell and a safe alternative without changing Confirmed Requirements.

**Exit:** a reviewed feasibility matrix with primary citations and local receipts, explicit unknowns and preservation results, plus one bounded R11-I entry contract or a concrete DESIGN_GATE. This checkpoint does not complete R11-I, L3, print production or Host acceptance. Human input is reserved for a material product preference, actual hands-on acceptance, platform/credential/account requirement, destructive action or release/publication.

## Qualification receipt — 2026-09-26

**Takeover and product authority.** `build/r11q/takeover-ack.json` records the clean baseline, exact local/tracking/remote SHA and single-writer transfer. Live [Requirements](https://app.notion.com/p/01b2ff7802fc410ab4cdb8fd26264495) were queried for the scoped IDs. Accepted [DEC-4](https://app.notion.com/p/3e0fd279a6f381e1a891de824f6d908d), [DEC-36](https://app.notion.com/p/3e0fd279a6f381ecae13f68127bd83c4), [DEC-37](https://app.notion.com/p/3e0fd279a6f381b5a9bde13cf3705d38), [DEC-38](https://app.notion.com/p/3e0fd279a6f381b0b00fe69c79b4fac9) and [DEC-42](https://app.notion.com/p/3e0fd279a6f381579807c7fcf770dd90) require editable round-trip assessment, native authority, bounded bake, AI as a bidirectional target, and no blind private-data reinjection. DEC-35/39/40/41 remain Proposed; no proposed architecture is treated as accepted. Current runtime metadata does not independently expose the dispatched `gpt-6-sol high` profile; effective Full access/approval-never, project path, Git state and no Nect GUI process were read back.

**Local capability.** `ARCHITECTURE.md` and source show native 0.14 as the authoring authority; `src/desktop/svg_import.cpp` provides bounded static SVG intake (no text, image, gradients or clips), `src/io.cpp` exports SVG, and `src/desktop/host.cpp` exports PNG. No AI/PDF/PSD decode or AI encode command, no AI API/MCP tool, and no OpenFX host adapter is present. The installed executables are Illustrator 2026 `30.8.2`, Acrobat DC `26.2.21931.0`, Resolve `21.1.0.14`, Poppler tools `26.07.0`, and ImageMagick `7.1.1-47`. Installation proves neither entitlement nor target-runtime acceptance. No OFX SDK or registered scratch adapter was found in the checked program directories. No dependency or product code was added.

| Cell | Result | Evidence and limit |
| --- | --- | --- |
| `Q-AI-IN` | `DESIGN_GATE` | Two unmodified Illustrator-supplied `.ai` files begin `%PDF-1.6` and `%PDF-1.5`; Poppler reads their PDF representations. The files are the installed Print profile (SHA-256 `F2112C926079C8081EB42B89EE07609A630836817C096F293D947929CC7FE0AF`) and MixedText sample (`368F72FE9C578435387F3E64611BA284442E942A5063D944C8E1BBC1249A8627`). MixedText rendered to a local PNG and yielded text via `pdftotext`, with an `Invalid Font Weight` warning. There is no Nect AI sniff/decode or native editable readback. The 14-byte invalid-header `.ai` (SHA-256 `70C5AD30A5B3EE860157D7655F98D54B46F67F23C99DAB4F53642202E3D7020C`) is rejected by `pdfinfo` exit 1. This is a PDF-reader negative only, not a Nect decoder test. |
| `Q-AI-OUT` | `PARTIAL / FAIL_READBACK` | A controlled Illustrator COM/ExtendScript run created an owned path and text, wrote `build/r11q/minimal-pdf-compatible.ai` (SHA-256 `EF3C69912EE788611849FB648D5BD50916E5F7D94BE3CDEAFA899129090EBF02`, 234471 bytes), and attempted cold reopen. `pdfinfo` reads PDF 1.6, one 300×200 pt page, Creator Illustrator 30.8; `pdftotext` reads `Nect Q`, and a Poppler render visibly shows the path/text appearance (PNG SHA-256 `4537932D81EF08A00625FFF97C070A04E4503B1747CE03FCB385CD1C94463737`). The Illustrator script failed `Error 1302: No such element` at named-path/text lookup after reopen, so named editable structure is **not verified**. Exact script/error receipt is `build/r11q/illustrator-probe-result.json`. A subsequently active Explorer-parented Illustrator process was left untouched; no attach/retry against that session. The derivative is an Adobe-generated scratch file, not a Nect AI encoder result. |
| `Q-AI-API` | `NOT_RUN / MISSING_IMPLEMENTATION` | Current Nect GUI/API/MCP exposes SVG import and PNG/SVG output, not AI inspect/decode/plan/encode/validate. The Illustrator cloud API is documented separately and needs service entitlement, credentials and upload URLs; those are not presumed available. |
| `Q-PDF-PRINT` | `DESIGN_GATE` | Acrobat is installed but Pro Preflight entitlement was not checked. No target job profile, ICC output intent, spot ink, PDF/X artifact, external preflight or printer receipt exists. Candidate print Requirements remain Candidate. |
| `Q-HOST` | `DESIGN_GATE` | Resolve is installed but no owned scratch OpenFX/Fuse registration, host session, parameter or render readback was attempted. No host capability is marked PASS. Standalone remains independent. |

**Official feasibility sources.** Adobe documents AI's PDF/PGF distinction, the PDF-compatible representation and editing-data option in [AI/PDF options](https://helpx.adobe.com/illustrator/kb/optimize-native-pdf-file-sizes.html) and [saving artwork](https://helpx.adobe.com/lu_en/illustrator/using/saving-artwork.html). [Illustrator scripting](https://helpx.adobe.com/id_id/illustrator/desktop/automate-visualize-data/automate-actions/install-and-run-scripts.html) documents the local bridge; the [Illustrator API](https://developer.adobe.com/firefly-services/docs/illustrator/) is a separate cloud beta with [credential setup](https://developer.adobe.com/firefly-services/docs/illustrator/getting-started/). The [PDFium C API](https://pdfium.googlesource.com/pdfium/+/refs/heads/main/public/fpdf_edit.h) exposes page Path/Text/Image/Form objects, but no PDFium runtime was installed or tested here; its [LICENSE](https://pdfium.googlesource.com/pdfium/+/refs/heads/main/LICENSE) and any bundled dependencies need a concrete distribution review before integration. The [OpenFX API](https://github.com/AcademySoftwareFoundation/openfx) and [Support license](https://github.com/AcademySoftwareFoundation/openfx/blob/main/Support/LICENSE) are public; the [Fusion Fuse SDK](https://documents.blackmagicdesign.com/UserManuals/Fusion_Fuse_SDK.pdf) and [Fusion manual](https://documents.blackmagicdesign.com/UserManuals/FusionManual.pdf) document host surfaces, not registration on this machine. [PDF/X-4 and X-6](https://pdfa.org/resource/iso-15930-pdfx/) are distinct targets; [Acrobat Preflight](https://helpx.adobe.com/acrobat/using/preflight-profiles-acrobat-pro.html) and [Output Preview](https://helpx.adobe.com/acrobat/using/previewing-output-acrobat-pro.html) can validate different print cells when the exact job contract and runtime are available.

**R11-I DESIGN_GATE, bounded next contract.** The first eligible implementation candidate is an Illustrator-independent **PDF-compatible AI input** slice, not an AI writer: sniff `%PDF-` and declared PDF/version, use a reviewed/licensed PDF object reader, lower exactly one owned Path plus one Text into native Session commands, and reject unknown/private/unsupported objects with scoped reasons. The output must retain source hash and native identity, preserve original bytes, support one atomic Undo and native 0.14 cold reopen, and expose inspect/decode results through the same GUI/API/MCP Session boundary. Positive fixture: the owned simple AI above only after its named-object discrepancy is independently resolved or replaced with a verified source. Negative fixtures: bad header, PDF-compatible AI with unsupported transparency/clip, and non-PDF/legacy AI must fail or report unsupported without partial Document mutation. This is an engineering candidate, **not yet an authorized code packet**: PDF object reader/version, actual editable text/path semantics, dependency license/package, source provenance policy and independent Illustrator structure readback remain to be fixed. No PDF rename or raster-only fallback may satisfy REQ-75/78/80. An optional Illustrator bridge may serve as an oracle/encoder later but does not satisfy Illustrator-independent REQ-75; Adobe cloud credentials/upload are a separate Human Gate. Print job targets and Resolve host format remain distinct later decisions. This concrete gate leaves Confirmed Requirements open while allowing the Mission Owner to continue the independent R02 Route lane.
