# R03-ENTRY-01 — one-sided repeated-gap Smart Snap

**Packet:** revision 1, Sol-approved bounded implementation. **Source:** `D:\Documents\Nect`, `codex/practical-alpha@1261818fa57638e5fad1fac1ed1393664406d28b`, clean, matching `origin` at freeze. **Authority:** current Nect Mission, DEC-71, [R03 Completion Route](https://app.notion.com/p/3e3fd279a6f381b4ba9dd2d1bf066e0f), Confirmed Must [REQ-33](https://app.notion.com/p/3e0fd279a6f381bcbfaccc7597c4da69), P02-C1 closure and repo `AGENTS.md`.

## Bounded result

Extend the existing Canvas drag candidate set with a one-sided repeated-gap candidate. For two *adjacent*, visible, eligible stationary targets A and B in the same entered Composition/Group scope, with nonoverlapping axis intervals `[a0,a1]`, `[b0,b1]` and gap `g=b0-a1 >= 0`, a moving object of axis size `w` may snap immediately **after** B to `[b1+g,b1+g+w]` or **before** A to `[a0-g-w,a0-g]`. The proposed interval must not overlap any stationary target on that axis. Evaluate x and y independently. Derive candidates from the frozen drag-start geometry, never a moving follower, hidden object, cross-scope object, or mutable preview. The source feature is min for after and max for before. Named feedback identifies A, B, repeated gap and side.

Keep the C1 distance-first, 6 logical-pixel inclusive threshold at frozen zoom, DPR independence and exact tie order `Guide > Grid > Artboard > Text baseline > object edge > object center > equal-gap`. Within equal-gap ties use stable IDs and feature order; keep the existing two-sided candidate. Candidate preview is transient, Escape/stale release is unchanged, successful release uses the existing `TranslateObjects` Session transaction and one Undo. No schema, core command, native 0.14, Grid/Guide/Margin, API or setting change. The existing API parity remains exact numeric placement and must not silently snap.

## Fixed oracles

- `R03-GAP-01`: A x `[100,140]`, B x `[180,220]` (`g=40`), moving M x `[400,480]` (`w=80`). A raw body drag that places M min at 263 (three du from the target) snaps it to `[260,340]`; feedback names A/B and “after”. The same fixture with raw M max at 57 snaps M to `[-20,60]`; feedback names A/B and “before”. At zoom 1, a raw candidate 7 du away does not snap. Repeat the same geometry on y; no x-only implementation. Release gives one revision/Undo, and native save/reopen retains the committed transform.
- `R03-GAP-NEG-02`: An overlapping A/B pair offers no repeated-gap candidate. If another stationary object occupies the proposed interval, that candidate is omitted. Hidden, moving, effective-follower, cross-Composition and out-of-scope objects cannot seed a pair or block placement. Point drags, exact numeric/API edits, cancel and stale drag do not gain this candidate or commit unintended state. A failed drag leaves Document bytes, revision and history unchanged.
- Candidate metadata/feedback is checked directly: a final coordinate alone is insufficient because an ordinary edge/center target may coincide. Test both axes and both sides against fixed expected coordinates, and preserve the existing two-sided equal-gap oracle.

## Ownership and verification

A fresh disposable Luna Max Worker owns only `src/desktop/canvas.{hpp,cpp}` and focused `tests/canvas_tests.cpp`; it must not commit, push, edit Notion, user files or unrelated code. Other agents may read the repository; do not revert their edits. Sol owns packet/docs, review, repair, actual Windows GUI/API/native acceptance, Git and Notion reconciliation. Build outputs stay under ignored `build/`.

Sol reviews the exact candidate for eligibility, collision omission, source-feature handling, priority, stale/cancel behavior and scope. Run focused Release Canvas tests, then proportionate serial regression because the change touches the drag hot path. On an owned disposable native file, verify actual 96-DPI Canvas feedback/drag/Undo, exact API readback, native save/cold reopen and recovery; measure the existing visible Snap scene against p95 <= 33.3 ms if available. Keep 200% interactive host limitations separate. Synchronize a coherent commit non-force and read back the exact remote SHA. This slice closes only the repeated-gap residual it proves; rotated, vertical and subsequent-line text baseline remain open, and REQ-33/R03 are not declared complete.

## Closure evidence (2026-09-26)

Sol reviewed the disposable Worker's exact Canvas/test diff: candidates use frozen eligible target bounds, reject a colliding stationary interval, preserve the two-sided candidate and tie order, and bind the after/before candidate to the moving min/max respectively. Focused Release `canvas_interaction` passed. Serial Release CTest passed **41/41**. `git diff --check` passed.

In the owned Windows desktop at 96 DPI, the disposable native fixture `build/r03-gui-x.nect` had A `[200,240]`, B `[280,320]` (40 du gap), and M `[600,680]` (80 du width). A real outline drag snapped M after B to `[360,440]`: API revision 2 reported `transform.tx=-240`. GUI Undo restored `tx=0` at revision 3. A second real outline drag snapped M before A to `[80,160]`: API revision 4 and saved native 0.14 both reported `tx=-520`. The GUI showed the corresponding positions and `Saved · revision 4`. A distinct cold-reopen Session `079dc37c-f375-448c-8349-b29f19071f28` reopened the same Document `9555775e-7dd7-4366-8a48-c0bbefe52dda` with `tx=-520`; reopened GUI showed M before A and `Saved · revision 0`. The saved native fixture SHA-256 was `5DDD815E40C0512551B70E248ADFF9116F246F290FF82932C3C43DE3E5773110`. The initial GUI drag from the rectangle interior deselected the object and made no revision; the subsequent drags used the visible outline. Transient drag feedback text was asserted in the focused Qt test; the atomic GUI drag tool captured the committed placement, not the in-flight label.

The existing visible Windows QWidget `canvas_benchmark --layout` passed its p95 <= 33.3 ms interval gate: lightweight max p95 18.09 ms and representative max p95 23.23 ms, at DPR 1. This measures the existing layout scene and does not prove GPU presentation or a 200% interactive host. API exact numeric placement, failed/cancelled drag no-mutation, both-axis feedback and negative eligibility are covered by focused Canvas tests and the preexisting Session/desktop contracts; the live GUI exercise above specifically covered x-axis positive placement and Undo. Recovery artifacts were written under ignored `build/r03-reopen-recovery/`, but no crash recovery was exercised in this slice.

**Residual:** Rotated, vertical and subsequent-line text baselines, plus the rest of R03 and REQ-33, remain open. No full line acceptance is claimed from this one-sided gap slice.
