# R02 / P03 property and save boundary — R02-ENTRY-01

Status: bounded implementation packet approved by the Mission Owner on 2026-09-26. The [Completion Route](https://app.notion.com/p/3e3fd279a6f381b4ba9dd2d1bf066e0f) R02 and [Worker packets](https://app.notion.com/p/3e3fd279a6f381ee8ab0ea5ea614c006) P03 own the product intent; this file records the current implementation boundary. Confirmed REQ-2/3/4/34/42/199 and cross-surface REQ-60/61/62 remain open unless their actual oracles pass. Candidate and deferred requirements do not silently become implementation scope.

## Type and authority matrix at `cc14d4c`

| Family | Authored authority | Name / read surface | Mutation, link, expression | Native persistence and residual |
| --- | --- | --- | --- | --- |
| Object transform, anchor, opacity; image dimensions; text numeric metrics; primitive parameters; point/Point Edit coordinates; operation and gradient numeric parameters | `Scalar{literal,binding?,expression?}` at stable `Ref` | `properties` gives type `number`, unit, local space, origin and authored/evaluated; `get` reads one; `resolve_name` resolves unique object or named color channel | `set`, `edit_properties`, `link_properties`, `unlink_properties`, `set_expression` share Session; GUI Inspector offers number, relative edit, pick source and expression draft; API/MCP forward the same commands | Native stores Scalar and stable refs. Generated point fallback is derived; Point Edit stores only overrides. Cycle, missing ref, unit, range and batch failures must reject atomically. |
| Named, paint and gradient-stop RGBA | Four Scalar channels plus a typed aggregate `ColorValue` view | `properties`/`color_properties` give type `color`, channel refs, usage, authored/evaluated and link; `get` reads aggregate; at the baseline **`resolve_name` rejected aggregate `color`** even for an unambiguous named color | `set_color`, `link_color`, `unlink_color` operate atomically across four channels; individual channels accept Scalar expressions; GUI Color tools use Session | Native stores channels/links, no second color authority. Aggregate Color has no single expression language; unsupported explicitly. |
| Text content, font family/locale/layout/direction/alignment; names; operation type/composite/fill rule/line styles; gradient type; blend | Dedicated strings/enums in source/operation/object structs | `inspect` and type-specific reads; absent from generic `properties` and `resolve_name` | Dedicated commands such as `update_text`, rename and operation options; no generic typed link/expression or shared Inspector expression field | Native retains literal data; a generic typed dependency contract and allowed source/target pairs remain DESIGN_GATE. Do not coerce to double. |
| Weight, italic, enabled/visible/isolated; structural IDs and references; object/tree/contour/stop order | Dedicated integer/bool/ID/structural fields | `inspect` and dedicated readers; absent from generic `properties` | Dedicated Session commands; no generic typed link/expression | Native retains fields; any typed dependency or ID migration needs an explicit contract. |
| Artboard frame and guide/grid/margin values | Numeric authored fields outside `Ref`/`Scalar` | `artboards` and layout-specific readers distinguish authored/evaluated | Dedicated artboard/layout commands; parent dimensions have their own typed link | Native retains them, but generic `properties`/expression linking is unsupported. |
| Effective world transforms, bounds, text layout and generated contours | Derived evaluated view | Read operations only | One-shot commands modify canonical authored fields; derived values are not separate persisted properties | Never write an evaluated projection back as authored state implicitly. |

The inventory follows `include/nect/core.hpp`, `src/core.cpp` (`property_index`, `properties`, `resolve_name`, `unit` and Session commands), `src/color.cpp`, `src/io.cpp` (`get`, `properties`, `artboards`, `inspect`) and `src/desktop/window.cpp`/`colors.cpp`. Core JSON-lines `capabilities.mcp=false` describes that server only; the desktop MCP adapter in `scripts/mcp_server.py` forwards to the same Session command model.

## First accepted slice: named aggregate Color discovery

Fix `resolve_name` so a unique named color and field `color` return its typed aggregate Color `Ref`, validated by the existing `color_channels` path. Keep the current Scalar channel lookup and its errors. Do not change the native schema, evaluator, Color commands or product color semantics. This closes one actual type discovery gap in the already accepted Color workflow; it does **not** claim all P03 types or generic Color expressions.

Positive oracle: create named colors with unique names and channel values; resolve the aggregate by name through the core API, read it with `get`, link it to a paint color via the existing typed `link_color` command, change the source, and check the target values, Undo, and `encode`/`decode` cold roundtrip. The same `resolve_name`/`get` path must be available from the desktop MCP forwarding boundary. GUI Color tools must continue to operate through Session. Negative oracles: unknown name, duplicate name, invalid Color field/owner, and a scalar-only `resolve_name` request still produce scoped errors; none commits a partial mutation. Preserve stable IDs through rename. Test only the changed boundary plus necessary existing Color tests.

Worker ownership: `src/core.cpp` and the relevant existing Color/API test file(s) only; no Git operation or native format change. The Mission Owner reviews the exact diff, runs focused validation, records residuals, and performs the sole commit/push/Notion update.

## R02-ENTRY-01 local result

The reviewed candidate adds one named-color aggregate branch in `resolve_name` and uses the existing `color_channels` validator. Existing Scalar name resolution and Color commands remain unchanged. `tests/color_tests.cpp` checks positive resolution, rename/stable ID, link propagation, scoped negative errors, unchanged revision/document on failed reads, Undo and native `encode`/`decode`. `tests/mcp_desktop_tests.py` checks the same named Color `resolve_name` followed by typed `get` over the actual desktop MCP adapter.

Release incremental builds for `color_tests` and `nect_desktop` passed. Serial `ctest --test-dir build -C Release -R '^color_contract$' --output-on-failure` and `-R '^mcp_desktop_contract$'` each passed 1/1 on 2026-09-26. The first Color test execution exposed a test sequencing mistake (ambiguous name was still present for a later missing-field oracle); the test was corrected and the final run passed. A fresh Debug target build stopped on pre-existing `src/text.cpp` WRL C3246 before linking Color tests; the Release contract result is the local verification claim. No actual GUI interaction was needed for the unchanged Color tools; this does not claim a new visual acceptance.

Residual: this resolves one typed discovery gap only. Non-Scalar string/enum/bool/ID dependencies, artboard/layout numeric `Ref` coverage, type-specific expression language, multi-target typed edit/link, migration policy and full P03 Save As acceptance remain open. No Confirmed Requirement is marked complete from this slice alone.

## R02-BOOL-01 — Text italic dependency contract (approved 2026-09-26)

**Entry:** `codex/practical-alpha@30065403aaca9edb8245499e57fa29e8c69a7c03`, clean and matched to tracking/fresh remote at takeover. Live Completion Route R02, P03 and Confirmed REQ-2/3/4/34/42/60/61/62/199 were read. This is one type slice; none of those Requirements or R02/P03 is thereby complete.

**Authority and address:** The existing `TextSource.italic` bool remains the authored literal. `Ref{object_id,"","text.italic"}` identifies only a Text object's Italic property; `TextSource.id`, object name and tree position are not addresses. An optional typed dependency (same-type source `Ref`) or typed expression is stored with that Text source, following the existing Scalar convention of one authored literal plus at most one driver. Only Text italic → Text italic is accepted. No number coercion, implicit relative offset, string/enum/ID source, or generic Scalar `LinkProperties` support is implied. Direct `UpdateText` may edit unrelated Text fields while preserving a driver; editing driven italic must require explicit unlink/replace. Deleting a referenced source must fail atomically until the link/expression is removed in the same batch.

**Expression subset:** v1 result type is bool. Accept `true`, `false`, `ref("object-id","","text.italic")`, and `!ref("object-id","","text.italic")` only, with exact stable-ID parsing and no name rebinding. No arithmetic, truthiness, filesystem, script, or general boolean grammar. Invalid syntax/type, missing source, self-reference and cycles reject without changing revision or authored bytes. A literal expression remains an authored expression, not a silent literal conversion. `resolve_name` may locate a uniquely named Text's bool Ref; rename/reorder must leave committed refs intact.

**Evaluation and surfaces:** Pure evaluation yields a bool map for Text italic without mutating literals. Text layout/render use evaluated italic; Inspector displays literal/driver/evaluated state and offers explicit unlink before literal edit. Core Session owns set/link/expression/unlink commands and Undo. The local JSON-lines API and desktop MCP adapter forward the same commands and `get`/`properties` typed `bool` metadata. A GUI affordance must not create a second editing path. Unsupported bool fields and operations return scoped errors. This slice does not claim an interactive GUI acceptance while the R03 Computer Use app/window target remains unavailable.

**Persistence:** Add only optional Text italic driver fields to native Text serialization and bump the native minor version once. Decode older 0.1–0.14 documents with no driver and preserve their literal italic value; a 0.15 save/reopen must retain literal, stable Ref and expression source exactly. Reject unknown/invalid driver data instead of dropping it. Do not migrate other fields or create a parallel property store. Save/reopen/readback is on an owned scratch document.

**Positive oracle:** Two differently named Text objects A/B, A italic=false and B italic=true. Resolve A's `text.italic`, link B to A, then set A=true; B evaluates true while its authored literal stays true. Rename and reorder A; B still points at A's ID. Unlink B freezes its current evaluated value, then changing A no longer changes B. Apply `!ref(A)` to B; A=true yields B=false, A=false yields B=true. One Undo restores the prior driver and evaluated value. Encode/decode and an independent desktop/API cold reopen retain the exact driver and evaluated bool. A GUI Text preview/Inspector check is required when an owned controllable window exists; until then that row is `LOCAL_WAIT`.

**Negative oracle:** Scalar `LinkProperties` against `text.italic`, a wrong-type source, duplicate target, missing target/source, self/cyclic link, invalid expression, direct literal edit while driven, and deletion of a referenced source all reject atomically: identical Document/native bytes, revision and prior evaluation. A failed draft never stores `false` or clears a prior driver. Multi-target bool batch, mixed-type links, other bool fields, Color aggregate expressions and broad expression language remain separate packets.

**Execution ownership:** A fresh disposable Worker may edit `include/nect/core.hpp`, `src/core.cpp`, `include/nect/io.hpp`, `src/io.cpp`, `src/desktop/window.cpp`, and focused existing tests needed for this exact contract. No Git, Notion, R03 or R11-I mutation. Sol reviews the exact diff, repairs, runs focused Release checks and scratch native/API readback, reconciles residuals, then alone commits and non-force synchronizes the checkpoint. The Worker must report any command/schema/runtime edge that makes this packet unsound before broadening the implementation.

## R02-BOOL-01 local result — 2026-09-27

The reviewed candidate adds typed `text.italic` discovery, bool link/expression/unlink Session commands, pure evaluation, Text layout/render use, Inspector controls, native 0.15 persistence and 0.1–0.14 literal migration. Driver references use object IDs; duplication remaps references inside the copied set. Invalid driver shapes and a 0.14 file carrying a 0.15 driver reject explicitly. The existing Text Inspector scroll assertion found a real layout-range collapse during editing; the Text form now publishes its rebuilt minimum height and the unchanged assertion passes.

Release builds of `nect_desktop`, `nect` and affected tests passed. A serial focused CTest subset passed **11/11**: assets, duplication, core, expression, artboard, text, text-authoring, history, canvas, window and process/schema contracts. `git diff --check` passed. An independent owned offscreen desktop Session created two Text objects, linked B's italic to A, updated A, replaced B's driver with `!ref(A)`, saved native 0.15, terminated the desktop, and opened that file in a different desktop Session. The reopened authored `inspect` document was exactly equal, `get` returned typed `bool` and evaluated `false`, and the saved file SHA256 stayed `efa0983d51321189bf4213723844530714a4d01ae3c4bef0b18a2b63d23b1d26`. The two Session IDs were `5a845a66-8571-488a-893d-ec0866ccbd5f` and `a6f59864-a730-43db-864b-e55c18601e1a`. This verifies desktop API/native readback; offscreen execution is not an interactive GUI acceptance.

Residual: String/enum/ID dependencies, other bool fields, generic multi-target typed edits/links, Color aggregate expressions, broader expression grammar, full P03 Save As/GUI acceptance and actual hands-on visual interaction remain open. The R03 interactive Snap row stays `LOCAL_WAIT`; R11-I remains at its separate DESIGN_GATE. No Confirmed Requirement or Route line is marked complete by this slice alone.

## R02-WEIGHT-01 — Text weight same-type link (approved 2026-09-27)

**Entry and scope:** Start from synchronized `codex/practical-alpha@d6a9eac0faf714b6a4c7ad308f27606737073dcc`, clean. P03 requires a type-by-type account of authored literal, link, expression and GUI/API/native surfaces. This slice covers one bounded integer link; integer expressions, offsets, mixed types, generic batches and other integer fields remain explicitly unsupported. It does not complete P03 or any Confirmed Requirement.

**Authority and address:** `TextSource.weight` remains the authored integer literal in [1,999]. `Ref{object_id,"","text.weight"}` identifies only a Text object's weight, using the stable Object ID. The optional driver is exactly one same-type Text weight Ref, with no coercion to a Scalar `double` and no second authored property store. Source and target must be distinct Text objects. Use dedicated Session `link_text_weight` and `unlink_text_weight` commands; an existing driver requires `replace_driver:true` for replacement. Unlink freezes the evaluated weight as the literal. `UpdateText` may edit unrelated fields while preserving the driver, and rejects an attempted literal change while driven. Delete of a referenced source rejects atomically unless the same batch first unlinks every surviving dependent.

**Evaluation and surfaces:** Evaluate weight as an integer through stable IDs with missing-reference, wrong-type, depth and cycle rejection. Text layout/render consume the evaluated weight; authored literal/driver remain unchanged. `properties`, `resolve_name` and `get` discover `text.weight` as type `integer`, unitless with range [1,999], and report literal/driver/evaluated separately. Generic Scalar source pickers and numeric expression editors must not silently offer this typed Ref. The Text Inspector shows evaluated weight, authored state and an explicit link/unlink affordance through the Session commands. Desktop MCP and local JSON-lines API forward the same commands. Interactive GUI acceptance is `LOCAL_WAIT` until an owned controllable window target is available; focused Qt interaction and offscreen API evidence must be labeled separately.

**Persistence:** Native 0.16 adds an optional `weight_driver:{"link":Ref}` only to Text source, retaining the existing integer literal. Native 0.1–0.15 decode as literal-only; files claiming an older version but carrying the new field reject. The 0.16 reader rejects unknown driver alternatives, wrong-type refs, missing source and cycles; writer omits the field when not authored. Save/reopen on owned scratch must preserve authored literal, stable link and evaluated result exactly. No unrelated native migration.

**Positive oracle:** Text A weight=700 and Text B weight=400. Link B to A, then set A=300; B evaluates and renders with 300 while its literal remains 400. Rename/reorder A keeps B's source ID. Unlink B freezes 300; later A changes do not affect B. One Undo restores the link and prior evaluation. Duplicating both A/B remaps B's copied link to copied A without changing originals. Encode/decode and a distinct owned desktop/API cold reopen preserve the same literal/link/evaluation.

**Negative oracle:** Generic Scalar `LinkProperties` or `SetExpression` against `text.weight`, wrong or missing Text source/target, self/cyclic link, stale revision, driven literal edit, source deletion, out-of-range authored weight and malformed/old-version native driver reject with unchanged revision, authored/native bytes and prior evaluation. A failed GUI draft must not clear the existing driver.

**Execution:** A fresh disposable Worker may modify core model/commands/evaluator, IO/native codec, Text evaluation adapters, Text Inspector and focused existing tests for this contract. The Mission Owner owns this packet/schema documentation, exact diff review, independent desktop/native check, Git and any Notion status. The Worker must not modify R03/R11-I or run Git mutation. If the link-only integer contract needs a broader property-model redesign, return the exact conflict before coding rather than creating a parallel system.

## R02-WEIGHT-01 local result — 2026-09-27

The reviewed candidate keeps the authored integer literal and adds one optional same-type stable Ref driver, typed discovery/readback, Session link/unlink, pure evaluation, Inspector controls and native 0.16 persistence. Text layout, Canvas, shape contours, transforms and Inspector use the evaluated weight. Generic Scalar pickers and expression editors exclude `text.weight`; integer expressions remain unsupported. The desktop MCP command description exposes the dedicated commands. Focused tests cover link propagation, identity through rename/reorder, Undo, duplication remap, driven edit and source-delete rejection, malformed/missing/cyclic native drivers and 0.15 literal migration.

Release build passed for `nect`, `nect_desktop`, `text_authoring_tests`, `duplication_tests`, `window_tests`, `text_tests` and `history_tests`. Serial focused CTest passed **7/7**: duplication, text, text-authoring, history, window interaction, process and desktop MCP contracts. `git diff --check` passed. `Test-Json -LiteralPath build/r02-weight-native-live/weight-linked.nect -SchemaFile schemas/native-v0.16.schema.json` returned `True`. An independent owned offscreen desktop Session saved linked Text weights, then a distinct desktop Session cold reopened native 0.16 with exact authored `inspect` equality, typed integer `get` evaluated 300 and retained target literal 400. The native SHA256 remained `e913079fbc1ce67091e04f88dd89893587cdd20caf25ed256d2134b0c14f32e3`. The Session IDs were `84ccf171-d33c-4483-8454-dd1fef8213a4` and `93096860-564a-41c6-a568-0865b6e36b78`. Qt offscreen Window tests and desktop API/native readback do not establish hands-on GUI acceptance.

Residual: Integer expressions, offsets and generic/mixed multi-target integer links remain unsupported, alongside the prior string/enum/ID, other bool, Color expression, Artboard/layout Ref, Save As and interactive GUI obligations. R03 GUI remains `LOCAL_WAIT`; R11-I stays at its separate DESIGN_GATE. No Confirmed Requirement or Route line is marked complete by this slice alone. Before further per-field drivers, audit the full P03 type-by-surface matrix and decide whether the remaining accepted types need a shared typed dependency abstraction, without creating a second authored property store.

## R02/P03 authored type and surface audit — 2026-09-27

The current [Completion Route](https://app.notion.com/p/3e3fd279a6f381b4ba9dd2d1bf066e0f) R02 calls for a type-specific expression/link table and save contract. [P03](https://app.notion.com/p/3e3fd279a6f381ee8ab0ea5ea614c006) requires every authored parameter to be accounted for across literal, link, expression, name discovery, GUI, API/MCP and native, with unsupported cells assigned a reason and follow-on packet. This is a source audit of native 0.16 at `9288869ec1cc17ae549380a28bafedd7488de270`, not a new runtime or hands-on GUI acceptance. `Ref` has stable object/point/field identity (`include/nect/core.hpp:24`); only the addressed property's type and command contract determine what it can do.

| Authored family and owner | Literal / link / expression | Name discovery and GUI | API/MCP and native | Residual / next boundary |
| --- | --- | --- | --- | --- |
| Ordinary numeric `Scalar`: Point six values, transforms/Anchor, primitive, Shape/gradient, opacity, Text numeric parameters, image size, named RGBA channels (`include/nect/core.hpp:53`, `:60`, `:103`, `:113`, `:148`, `:181`, `:270`) | Literal, typed-unit Scalar binding and bounded numeric expression; generated points are derived unless explicitly corrected. Integer-like generator counts remain constrained Scalars, not a general integer value type. | Scalar `resolve_name`, Inspector numeric/reference/expression controls; source picker excludes Text typed refs (`src/core.cpp:587`, `src/desktop/window.cpp:2608`). | `properties`/`get`, Session batch commands, JSON-lines API and desktop MCP; native persists literal/binding/expression. | Keep type/unit/cycle and atomic multi-target semantics. Generated values and authored corrections must remain distinct. |
| Color aggregate over existing RGBA Scalars (`include/nect/core.hpp:261`, `src/color.cpp`) | Authored channels plus named Color link/commands; no aggregate Color expression. | Named aggregate `color` discovery and Color Tools; a color is not four independent UI guesses. | Typed `get`/`properties` and native channel/link readback. | Color expression result, range/conversion and cross-surface draft policy need a separate contract. |
| Text italic `bool` (`include/nect/core.hpp:101`) | Literal, same-type stable Ref link or bounded bool expression. | `text.italic` discovery and Inspector control; interactive GUI acceptance outstanding. | Typed `get`/`properties`, dedicated Session/API/MCP commands; native 0.15+ optional driver. | Other authored booleans have no corresponding typed dependency contract. |
| Text weight `integer` [1,999] (`include/nect/core.hpp:99`) | Literal or same-type stable Ref link; no integer expression or offset. | `text.weight` discovery and Inspector control; interactive GUI acceptance outstanding. | Typed `get`/`properties`, dedicated Session/API/MCP commands; native 0.16 optional link driver. | Other integer fields and integer expression/batch semantics remain open. |
| Other booleans: contour closed, PointEdit/operation/gradient/mask enabled, visibility, isolation and Artboard parent dimension flags (`include/nect/core.hpp:68`, `:121`, `:138`, `:164`, `:172`, `:195`, `:203`) | Authored literals and scoped commands; no general bool link/expression. | Bespoke controls where exposed; absent from typed `properties` and `resolve_name`. | Command or `inspect` readback and native fields, not generic typed `get`/MCP property discovery. | Classify which are actual linkable parameters versus topology/enable controls before adding drivers. |
| Text content/family/locale and names/resource labels (`include/nect/core.hpp:79`, `:97`, `:178`, `:208`, `:235`, `:256`, `:268`) | Authored strings via object-specific commands; no string Ref/link/expression. | Text/name editors exist, but no typed string discovery. | Scoped API/MCP commands and `inspect`; native stores strings. | String source/target identity, length/Unicode/IME draft, missing source and render propagation need a bounded contract. Names and asset locators are identity/metadata, not automatically linkable text. |
| Enumerations: Text layout/direction/alignment, Shape/gradient options, blend/mask/Guide axis and asset mode (`include/nect/core.hpp:80`, `:98`, `:136`, `:149`, `:165`, `:171`, `:209`) | Authored validated choices via scoped commands; no enum link/expression. | Bespoke selectors where exposed; no generic enum name discovery. | Scoped API/MCP/`inspect` and native fields. | Domain-specific allowed values, conversion and source deletion must be defined before linking; arbitrary string expressions are not enum values. |
| Stable IDs and references: hierarchy, transform parent, mask source, image asset, Collection members and Artboard parent (`include/nect/core.hpp:180`, `:194`, `:201`, `:256`) | Explicit structural/reference commands, no generic value link/expression. | Selection/pickers are domain operations, not numeric property pickers. | API/MCP commands and native refs with domain validation. | No implicit ID coercion or name rebinding. R04/R10 ownership and reference semantics remain separate. |
| Artboard frame, layout/margin/grid and Guide numeric values (`include/nect/core.hpp:207`–`:248`) | Authored `double`/count fields; Artboard parent-size is a specialized width/height dependency. No Scalar binding or expression. | Artboard/layout controls and separate `artboards` readback; no ordinary property `Ref` discovery. | Scoped Session/API/MCP commands, `artboards` readback and native 0.16; no typed `get`/`properties` for these fields. | Frame/Guide/layout refs need composition/space/range/cycle rules and a migration plan. Do not silently replace existing raw frame fields with Scalars. |

Surface key: **Y** implemented for this family, **S** scoped/specialized command or readback only, **N** unsupported. GUI Y denotes an existing control, not hands-on acceptance.

| Family | Literal | Link | Expression | Name discovery | GUI | API/MCP | Native |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Scalar numeric | Y | Y | Y | Y | S | Y | Y |
| Color aggregate | Y | Y | N | S, named | S | Y | Y |
| Text italic bool | Y | Y | Y, bounded | Y | Y | Y | Y |
| Text weight integer | Y | Y | N | Y | Y | Y | Y |
| Other booleans | Y | N | N | N | S | S | Y |
| Text strings / names | Y | N | N | N | S | S | Y |
| Enumerations | Y | N | N | N | S | S | Y |
| Structural IDs / references | Y | S, domain relation | N | N | S | S | Y |
| Artboard / layout / Guide numerics | Y | S, parent size only | N | N | Y | S | Y |

Derived text layout, bounds, generated points without corrections, evaluated Artboard sizes, Canvas overlay state and one-shot command arguments (for example Align/Distribute spacing) are **not** authored property slots. Raster payload bytes, immutable metadata and linked asset locators are resource ownership, not linkable text/number parameters. `src/core.cpp:385`–`:445` indexes Scalar slots, while `src/core.cpp:553`–`:608` adds only Text italic/weight to generic discovery at the audited HEAD. `src/io.cpp:1178`–`:1196` shows generic property enumeration; `src/io.cpp:1225` exposes Artboards separately, and `src/io.cpp:931` serializes the authored document. Native 0.16's Text and Artboard shapes are in `schemas/native-v0.16.schema.json:1524`–`:1614` and `:350`–`:455`. `src/desktop/window.cpp:1888`–`:1890` has Text enum controls. Desktop Save As exists (`src/desktop/window.cpp:471`), while full P03 Save As source-preservation and GUI acceptance remain separately unproven.

**Boundary decision:** The two Text drivers establish typed evaluation but are per-field adapters. More per-field drivers across string, enum, ID and Artboard fields would repeat discovery, dependency traversal, deletion, duplication, API, Inspector and migration rules. A shared *descriptor/evaluation contract* may be useful, but a second authored property store or whole-document schema rewrite is not justified. First freeze a read-only typed discovery slice for existing Text string/enum literals, including explicit capability flags (`link:false`, `expression:false`) and stable `Ref` validation, without changing authored bytes or native version. This gives clients an honest inventory and a reusable metadata shape before any new dependency or save semantics. Its source owner remains `TextSource`; later string/enum/ID link packets require their own positive/negative oracle and authority check.

### R02-TEXT-DISCOVERY-01 — bounded next packet

**Scope:** Publish existing Text `content`, `family`, `locale` as `string` and `layout`, `direction`, `alignment` as `enum` through `Ref{Text object ID,"","text.<field>"}`. `TextSource` remains the sole authored owner. This packet changes only read-only property discovery and validation; it adds no driver, editing command, native field/version, second value store or new GUI control. The specialized existing `UpdateText` command remains the edit path. Object/Text IDs, names, transform parent and asset locators are outside this slice.

**Positive oracle:** For two differently named Text objects with distinct content and enum choices, `properties` lists exactly six new refs per Text with correct type, literal/evaluated value, enum choices and `link:false`/`expression:false`; `get` agrees. `resolve_name` yields the stable ID Ref for a unique named Text and survives rename/reorder. The local JSON-lines and desktop MCP adapter return the same typed result from the same Session. Encode/decode of the unchanged native 0.16 document preserves exact authored values and no discovery operation changes bytes or revision. Existing Text Inspector values remain coherent with the readback.

**Negative oracle:** Missing/duplicate name, wrong object kind, unknown `text.*` field and nonempty point reject with scoped errors; no guessed/default value. Scalar `link_properties`, `set_expression` and numeric source pickers reject or omit these refs rather than coercing to `double`. `resolve_name`/`get`/`properties` must leave Document bytes and revision unchanged. Tests should cover those semantic failures and one actual API/MCP readback, without claiming hands-on GUI acceptance.

**Execution ownership:** A fresh disposable Worker may modify only core property addressing/discovery, IO `get`/`properties` serialization, the desktop numeric source picker and focused existing tests. No native serializer/schema or unrelated model mutation, no Git/Notion operation. The Mission Owner reviews the diff, runs focused Release validation and independent API/native readback, then alone commits and non-force synchronizes. If exposing these read-only refs requires a parallel store or broad command refactor, return that exact conflict before implementation.

### R02-TEXT-DISCOVERY-01 local result — 2026-09-27

The reviewed candidate exposes the six existing Text source values through stable typed refs in `properties`, `get` and `resolve_name`. It reports string or enum, the literal/evaluated value, exact enum choices and explicit `link:false` / `expression:false`. It reads `TextSource` directly, leaving `UpdateText` as the authoring command and native 0.16 unchanged. The numeric/expression source pickers filter by actual Scalar evaluation membership, retaining numeric Text parameters while omitting the new typed refs. Unknown field, wrong object kind, invalid point and Scalar command misuse reject without authored changes.

Release build of `text_authoring_tests`, `batch_ui_tests` and `nect_desktop` passed. Focused `text_authoring_contract`, `batch_ui_interaction` and `mcp_desktop_contract` passed **3/3**. An initial MCP test arrangement reordered all roots and disrupted a later grouping fixture; the test was narrowed to move only the Text object, then all three passed. `git diff --check` passed. The Mission Owner separately built `nect` Release and opened the existing native 0.16 weight fixture in a new `nect.exe --serve` process: `properties` returned six matching Text A refs, all six typed `get` values matched their authored literals (`string,string,string,enum,enum,enum`), and the native SHA256 stayed `e913079fbc1ce67091e04f88dd89893587cdd20caf25ed256d2134b0c14f32e3`. This is CLI/API and offscreen desktop MCP/Qt evidence, not a hands-on GUI acceptance.

Residual: discovery alone adds no string/enum dependency, expression, generic multi-target typed editing or Save As acceptance. The baseline audit table above records the entry state; for these six Text fields, name discovery and typed API/MCP now pass locally. Other string/enum fields, booleans, Artboard/layout refs, Color expressions and interactive GUI obligations remain. No Confirmed Requirement, R02 or P03 is marked complete from this slice.
